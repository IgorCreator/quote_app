<h2 class="text-center mt-4">Category: <%= @category.name.capitalize %></h2>

<% if user_signed_in? %>
  <div class="text-center mt-4">
    <%= link_to "Edit category name", edit_category_path(@category), class: "btn btn-outline-info" %>
  </div>
<% end %>

<h3 class="text-center mt-4">Quotes</h3>

<%= will_paginate @quotes, list_classes: %w(pagination justify-content-center) %>
<%= render 'quotes/list' %>
<%= will_paginate @quotes, list_classes: %w(pagination justify-content-center) %>
